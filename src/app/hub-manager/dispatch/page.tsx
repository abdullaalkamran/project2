export const metadata = {
  title: "Dispatch | Hub | Paikari",
  description: "Manage lot dispatch to delivery points.",
};

import DispatchClient from "./DispatchClient";

export default function HubDispatchPage() {
  return <DispatchClient />;
}
